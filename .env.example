# Supabase — live order backend (optional). Copy values into .env.local for local dev,
# and into GitHub Actions secrets (SUPABASE_URL / SUPABASE_ANON_KEY) for deploys.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
