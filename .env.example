# URL of the Akri Bakes order backend (FastAPI).
# When set, orders are saved centrally via the backend API.
# When unset, the site falls back to per-browser localStorage.
NEXT_PUBLIC_API_URL=https://your-backend-url
